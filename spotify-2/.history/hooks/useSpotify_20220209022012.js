import React from 'react'

function useSpotify() {
    const { data: session, status } = useSession();
  return (
    <div>
      
      useEffect(() => {
          return () => {
              effect
          };
      }, [sess])

    </div>
  )
}

export default useSpotify
